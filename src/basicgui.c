#include <stdio.h>
#include <stdlib.h>
#include <unistd.h>
#include <dirent.h>
#include <errno.h>
#include <math.h>
#include <string.h>

#include "gtkpfuncs.h"

void main (int argc, char *argv[]) {
#if 0
	echotest("teststring 111");

	printf("Press enter to continue\n");
	char enter = 0;
	while (enter != '\r' && enter != '\n') {
		enter = getchar();
	}
	printf("Thank you for pressing enter\n");

	exit(EXIT_SUCCESS);

#endif
}
